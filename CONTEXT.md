# SeedSync Development Context (Handover)

## 🎯 Current Status: PRODUCTION PILOT READY
The system has been extracted from the monorepo and stabilized for production scale. 

### 🛠️ Key Improvements & Fixes (Last Session)
1. **The "Lead Leak" Fix**: Implemented a strict sync-lock in the scheduler. The `last_row_index` (pointer) only advances if the database insertion is 100% successful.
2. **Special Characters Resolver**: Added automatic sanitization of spreadsheet headers (e.g. `Atendido?` -> `atendido`). This prevents schema mismatches when sheet names contain symbols.
3. **Quota Protection (429 Fix)**: Implemented concurrency throttling (max 2 sheets at a time) and exponential backoff retries for Google Sheets API limits.
4. **Duplicate Prevention**: Added a unique-batch constraint in the scheduler and a fingerprint check in the ingester to prevent double-firing notifications.
5. **Fail-Safe Ingestion**: Switched to a fail-fast model where the batch aborts if duplication checks or schema checks fail, preventing accidental re-processing of old leads.

### 🔌 Integration Details
- **Database**: Supabase (PostgREST) using the `seed_sync` schema profile.
- **Notifications**: Z-API (WhatsApp) with contact mapping.
- **Auth**: Service Role Key used for internal worker operations.
- **Sheet Monitoring**: Monitors `BVK_PREV` (currently set to focus on row 118+).

### 🚀 Immediate Next Steps
1. **GitHub Push**: The user is currently pushing this to `github.com/3F-Tech/SeedSync`.
2. **Scaling**: To add more clients, re-activate them in the Supabase `source_configs` table.
3. **Deployment**: Move from local execution to a persistent server (VPS/Docker).

---
*Generated by Antigravity AI on 2026-04-10 - Ready for new session.*
