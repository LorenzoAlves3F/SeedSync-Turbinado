kind: backend
runtime: python
install_cmd: "python3 -m venv venv && ./venv/bin/pip install -r requirements.txt"
build_cmd: ""
start_cmd: "./venv/bin/python run.py"
pm2_name: seedsync-backend
port: 8000
requires_nginx: true
domains:
  - api-seedsync.3fventure.tech
healthcheck: /health
