kind: frontend
runtime: node
install_cmd: "cd admin && npm install"
build_cmd: "cd admin && VITE_API_URL=https://api-seedsync.3fventure.tech npm run build"
start_cmd: "cd admin && npm run preview"
pm2_name: seedsync-admin
port: 4000
requires_nginx: true
domains:
  - seedsync.3fventure.tech
healthcheck: /
