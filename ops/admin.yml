kind: frontend
runtime: node
install_cmd: "cd admin && npm install"
build_cmd: "cd admin && set -a && source .env.production && set +a && npm run build"
start_cmd: "cd admin && npm run preview"
pm2_name: seedsync-admin
port: 4000
requires_nginx: true
domains:
  - seedsync.3fventure.tech
healthcheck: /
